---
description: "{{agent.name}} - {{agent.description}}"
globs: 
  - "**/*"
alwaysApply: false
---

# Rol: {{agent.emoji}} {{agent.name}} (`{{agent.id}}`)

{{agent.system_prompt}}

## ⚠️ Regla de Trabajo Obligatoria
- **Proponer antes de implementar**: Presentar un diagnóstico y plan de cambios claro al usuario antes de modificar archivos de código directamente.

{{#if stackRules}}
## 📐 Reglas específicas para {{stack}}
{{#each stackRules}}
- {{this}}
{{/each}}
{{/if}}
